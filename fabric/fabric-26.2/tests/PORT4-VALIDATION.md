# port.4 验证记录

2026-09-08，Java 25、Minecraft 26.2、Fabric 0.19.3，正式 JAR。

- build 通过，43 项离线回归覆盖：存储、压缩、自动缩放、原文件不变、关闭预压缩、读取额度、GIF/WebP 动图拒绝、超过旧 10 MiB 的 PNG、永久保留跨百年清理和重启、满额拒绝不删除旧图、动画时序。
- 隔离 HMCL 客户端保留原有模组，MCPFabric 调用真实上传入口；本地独立服务器单张限制为 100000 字节。
- 788100 字节 PNG 自动压缩为 44538 字节 WebP，分块上传成功，服务器保存 44538 字节，返回 mcimage 引用；没有二次有损压缩。
- 修复实机 ImageIO SPI 无法发现内嵌 WebP 编码器的问题，显式实例化编码器/解码器后复测通过。
- 10 项实际网络协议断言通过，见 evidence/port4-protocol-results.json。

预压缩依旧受像素预算与客户端 maxSourceBytes 保护。动图不转码。生产服务器验证以部署记录为准。
